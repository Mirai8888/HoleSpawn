# HoleSpawn — Rabbit hole / ARG generator (Twitter/X only)
# Python 3.10+

# NLP & sentiment (no API keys)
vaderSentiment>=3.3.2

# NLP layer (optional; Discord hybrid pipeline)
# pip install spacy && python -m spacy download en_core_web_sm
spacy>=3.7.0
nltk>=3.8.0
scikit-learn>=1.3.0
gensim>=4.3.0

# AI API (use at least one; keys from env)
anthropic>=0.39.0
openai>=1.0.0
google-genai>=0.5.0

# Optional: Apify Twitter scraper (--twitter-username); requires APIFY_API_TOKEN
apify-client>=1.6.0

# Optional: Agent social engagement (Discord DMs, Twitter DMs/replies)
discord.py>=2.0.0
tweepy>=4.0.0

# Logging, progress, config
loguru>=0.7.0
tqdm>=4.66.0
pyyaml>=6.0
jsonschema>=4.0.0

# Network analysis (graph profiling: Louvain communities)
networkx>=3.0
python-louvain>=0.16
numpy>=1.20.0
scipy>=1.7.0

# Dashboard & C2 (optional)
flask>=3.0.0
flask-cors>=4.0.0
sqlalchemy>=2.0.0
bcrypt>=4.0.0

# Site builder HTML sanitization
bleach>=6.0.0

# Utilities
python-dateutil>=2.8.2
python-dotenv>=1.0.0

# Tests (optional)
# pytest>=7.0.0
