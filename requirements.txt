# HoleSpawn — Rabbit hole / ARG generator
# Python 3.10+

# NLP & sentiment (no API keys)
vaderSentiment>=3.3.2

# AI API (use at least one; keys from env)
anthropic>=0.39.0
openai>=1.0.0
google-genai>=0.5.0

# Audience mapping (following list, Bluesky/Mastodon)
requests>=2.28.0

# X.com (Twitter) scrape: user tweets + following list (Nitter-backed)
ntscraper>=0.1.0
beautifulsoup4>=4.12.0

# Utilities
python-dateutil>=2.8.2
